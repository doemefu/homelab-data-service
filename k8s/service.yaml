apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: apps
  labels:
    app: data-service
spec:
  selector:
    app: data-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
  type: ClusterIP
