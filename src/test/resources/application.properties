# Test-only overrides, loaded on top of src/main/resources/application.yaml.
# No @Scheduled trigger fires during tests; collectors are invoked explicitly (SchedulingConfig).
netmon.scheduling.enabled=false
